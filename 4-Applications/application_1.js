/*
=======================================================================================================================================================

https://leetcode.com/studyplan/top-interview-150/

=======================================================================================================================================================

find یا شیء استفاده کن اگر داده‌هایت در آرایه‌اند و مدام می‌خواهی یک عنصر را با یک کلید خاص پیدا کنی، جستجوی خطی Map در آرایه از find به‌جای 

تبدیل کن تا دسترسی مستقیم و سریع داشته باشی Map  کند است. داده‌ها را به ساختار کلید-مقدار

=======================================================================================================================================================

find :

پیچیدگی زمانی O(n) –  یعنی هر بار کل آرایه پیمایش می‌شود

بسیار کند می‌شود UI برای یک بار جستجو شاید مشکلی نباشد، اما برای جستجوهای مکرر در لیست‌های بزرگ یا داخل رندر 

=======================================================================================================================================================

Map :

پیچیدگی زمانی O(1) :  یا دسترسی با کلید get برای 

مستقیم به عنصر می‌رسی، بدون پیمایش

=======================================================================================================================================================

// روش کند (آرایه)

const users = [{id:1, name:"Ali"}, {id:2, name:"Sara"}];
const user = users.find(u => u.id === 2); // O(n)

// روش سریع (Map)

const usersMap = new Map();
usersMap.set(1, {id:1, name:"Ali"});
usersMap.set(2, {id:2, name:"Sara"});
const user = usersMap.get(2); // O(1)

=======================================================================================================================================================

Map الگوی عملی: ایندکس کردن آرایه به 

تبدیل کن Map به صورت آرایه می‌آید. برای جستجوهای بعدی یک بار آن را به  API معمولاً دیتا از 

const products = [{id:101, title:"Keyboard"}, {id:102, title:"Mouse"}];

const productMap = new Map();
products.forEach(p => productMap.set(p.id, p));

// حالا هر بار می‌توانی با O(1) بگیری
console.log(productMap.get(102)); // Mouse

عالی است map/filter/reduce  آرایه برای نمایش لیست و عملیات 

تبدیل کن (Map یا Object) برای جستجوی مکرر بر اساس یک کلید مشخص، داده‌ها را به یک ساختار کلید-مقدار 

=======================================================================================================================================================
======================================================================================================================================================
*/