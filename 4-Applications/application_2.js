/*
=======================================================================================================================================================
=======================================================================================================================================================

Debounce و Throttle 

Queue دو الگوی کنترلی برای محدود کردن نرخ اجرای تابع در مواجهه با رویدادهای پشت‌سرهم هستند. از نظر مفهومی مانند مدیریت یک صف 

از رویدادها عمل می‌کنند، اما پیاده‌سازی آن‌ها در جاوااسکریپت معمولاً با تایمرها و متغیرهای وضعیت انجام می‌شود، نه با یک کلاس صف واقعی

=======================================================================================================================================================

Debounce: تا وقتی رویدادها پشت سر هم می‌رسند، اجرا را عقب می‌اندازد. فقط آخرین رویداد (پس از قطع شدن جریان) اجرا می‌شود

مثال: تایپ در فیلد جستجو، تغییر سایز پنجره

Throttle: در هر بازه زمانی مشخص، فقط یک بار تابع را اجرا می‌کند. رویدادهای اضافی در آن بازه نادیده گرفته می‌شوند

مثال: اسکرول، حرکت موس

=======================================================================================================================================================

// Debounce
function debounce(fn, delay) {
  let timer;
  return function (...args) {
    clearTimeout(timer);
    timer = setTimeout(() => fn.apply(this, args), delay);
  };
}

// Throttle (با استفاده از زمان آخرین اجرا)
function throttle(fn, delay) {
  let lastCall = 0;
  return function (...args) {
    const now = Date.now();
    if (now - lastCall >= delay) {
      lastCall = now;
      fn.apply(this, args);
    }
  };
}

در این الگوها (سطح مفهومی) Queue نقش صف 

Debounce : در  هر رویداد جدید تایمر قبلی را باطل می‌کند (انگار عنصر قبلی صف دور ریخته می‌شود) و فقط آخرین رویداد بعد از سکوت، پردازش می‌شود

Throttle : در  رویدادهایی که در فاصله زمانی ممنوعه می‌رسند مستقیماً رد می‌شوند (از صف حذف می‌شوند) و فقط اولین رویدادِ هر بازه اجرا می‌شود

رویدادها مانند یک صف ورودی هستند که یکی‌یکی می‌رسند. ما تصمیم می‌گیریم کدام‌ها را نگه داریم و کدام را رد کنیم

=======================================================================================================================================================
=======================================================================================================================================================

*/